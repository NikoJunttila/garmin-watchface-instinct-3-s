<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- App definition: id, type, name, entry class, and target devices. -->
<iq:manifest version="3" xmlns:iq="http://www.garmin.com/xml/connectiq">
    <iq:application
        id="33e41ff8b61548ad8ccda78c6d8182a0"
        type="watchface"
        name="@Strings.AppName"
        entry="HelloGarminApp"
        launcherIcon="@Drawables.LauncherIcon"
        minApiLevel="6.0.0">

        <!-- Watches this builds for. -->
        <iq:products>
            <iq:product id="instinct3amoled45mm"/>
            <iq:product id="instinct3amoled50mm"/>
        </iq:products>

        <!-- System permissions (none needed yet). -->
        <iq:permissions/>

        <!-- Supported languages. -->
        <iq:languages>
            <iq:language>eng</iq:language>
        </iq:languages>

        <!-- Shared code libraries (none used). -->
        <iq:barrels/>

    </iq:application>
</iq:manifest>
