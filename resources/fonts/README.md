# Fonts

Three custom **1-bit bitmap fonts** for the watch face, declared in `fonts.xml`:

- `nordic_hero.{fnt,png}` — the big "HH:MM" time. Digits + `:` only, mono-width digits
  (so the clock never shifts position between minutes).
- `nordic_label.{fnt,png}` — stat values, the date line, and heart-rate bpm. Digits,
  A–Z, and the punctuation the face draws (`. , : % - /`).
- `nordic_small.{fnt,png}` — the watch-battery percentage in the bottom status row.
  Digits + `%` only (a smaller size than the label font, which looked oversized there).

## Why bitmap + `antialias="false"`

The Instinct 3 Solar is a 1-bit MIP panel (`antiAliasedFontSupport=false`, palette =
black/white/transparent). Vector fonts and grayscale anti-aliasing are unsupported, so
the glyphs are pre-rendered and **hard-thresholded to pure black/white**.

## Regenerating

Generated by `tools/genfont.py` (Python + Pillow). Don't hand-edit the `.fnt`/`.png`;
re-run instead:

```sh
task font
```

The generated files are committed, so a normal `task build` does **not** require Pillow.

## Typeface & license

Rendered from **Noto Sans Condensed** (Black for the hero, SemiBold for labels),
licensed **Apache-2.0** — see `LICENSE-NotoSans-Apache-2.0.txt`. Apache-2.0 permits
redistributing the rendered bitmaps; this license file is included for attribution.
